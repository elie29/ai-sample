# Prompt - implement one task from a plan

Read docs/superpowers/plans/NNN-<feature-name>.md.

The current task is task <N>. Implement ONLY this task:

- respect the scope (files listed in the plan);
- follow CLAUDE.md and the relevant rule files;
- run the task's verification command and show me the real output;
- update the plan file: status, decisions taken, blockers.

Do not start the next task. Do not commit.
