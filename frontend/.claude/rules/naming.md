# Naming conventions

- One business concept, one name - identical to the backend and the specification.
- Components: `feature-name.component.ts`; services: `feature-name.service.ts`.
- Selectors prefixed with the application prefix.
- Files and folders: kebab-case. One topic per rule file.
